---
version: 5

defaults:
  data_hash: json_data
  datadir: "/etc/puppetlabs/code/hieradata"

hierarchy:
  - name: "Common data"
    path: "common.json"
#:backends:
#  - json
#:hierarchy:
#  - common

#:json:
#  :datadir: /etc/puppetlabs/code/hieradata
